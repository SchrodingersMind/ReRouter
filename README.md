# Tiny proxy server



## Build
```shell
mkdir build && cmake ..
cmake -build . 
```

## Usage
```shell
./build/ReRouter <port>
```

And configure HTTP proxy settings in your browser

`Warning`: HTTPS proxy isn't supported 